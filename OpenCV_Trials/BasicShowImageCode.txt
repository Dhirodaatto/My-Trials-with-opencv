
#include <opencv2/core.hpp>
#include <opencv2/imgcodecs.hpp>
#include <opencv2/highgui.hpp>
//#include <opencv2\video\background_segm.hpp>
//#include <opencv2\video\tracking.hpp>
#include <iostream>
#include <Windows.h>

using namespace cv;
using namespace std;
int main(int argc, char** argv)
{
    String imageName("C:/Users/dhiro/OneDrive/Pictures/Screenshots/Output to warp.png"); // by default
    if (argc > 1)
    {
        imageName = argv[1];
    }
    Mat image;
    image = imread(samples::findFile(imageName), IMREAD_COLOR); // Read the file
    if (image.empty())                      // Check for invalid input
    {
        cout << "Could not open or find the image" << std::endl;
        return -1;
    }
    /** Resizing to an X by Y matrix
    resize(image, image, Size(X, Y));

    */
    cout << "Image size = " << image.size();
    // !!!!
    // ALERT : When creating named window, pass WINDOW_NORMAL as a parameter otherwise entire image is NOT SHOWN !!
    namedWindow("Display window", WINDOW_NORMAL); // Create a window for display.
    // !!!!

    
    imshow("Display window", image);                // Show our image inside it.
    waitKey(0); // Wait for a keystroke in the window
    return 0;
}